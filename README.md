# Sentinel

Short term goals:
- An "AirCanada" demo working.

TODO:
- `askUserAskableTool :: Tool db` should probably be split into many tools (one per declared askable fact), so that we can assign proper schemas.
